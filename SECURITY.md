# 安全与运行边界

当前版本面向受信任的本地单用户环境，默认监听环回地址。没有完整的多租户认证授权；配置读取与媒体静态服务需要生产加固。请勿直接暴露到公网。

不要在 Issue、截图、日志或提交中包含 API Key、个人项目、上传素材。真实配置、生成产物、依赖和缓存已在 .gitignore 排除。

生产部署前需要身份认证、项目授权、脱敏配置接口、受限媒体访问、上传校验、CORS 白名单和审计。

发现安全问题时优先使用 GitHub 私密漏洞报告（如仓库已启用）；不要公开提交可用凭据或他人数据。
